import { StyleSheet } from 'react-native'

export const Input = StyleSheet.create({
    input : {
        height : 45,
        borderColor:"#13BA56",
        borderRadius : 12,
        borderWidth:0.8,
        backgroundColor : '#fff',
        alignSelf:'stretch',
        paddingHorizontal : 20,
        borderWidth: 1.0,
        marginBottom: 10
      }
})
